<!--
 * @Author: 胖胖很瘦
 * @Date: 2025-03-17 13:06:13
 * @LastEditors: 胖胖很瘦
 * @LastEditTime: 2025-03-29 16:17:06
-->
<footer class="site-footer">
  <div class="footer-inner container">
    <!-- <div class="footer-content">
      <div class="footer-section about">
        <h3 class="footer-title"><%= config.title %></h3>
        <p class="footer-description">
          <%= config.description || '基于Hexo的微信风格主题博客' %>
        </p>
        <% if (theme.footer && theme.footer.social_links) { %>
          <div class="social-links">
            <% for (let platform in theme.footer.social_links) { %>
              <a href="<%= theme.footer.social_links[platform] %>" class="social-link" target="_blank" rel="noopener noreferrer" title="<%= platform %>">
                <% if (platform === 'github') { %>
                  <i class="fab fa-github"></i>
                <% } else if (platform === 'twitter') { %>
                  <i class="fab fa-twitter"></i>
                <% } else if (platform === 'facebook') { %>
                  <i class="fab fa-facebook-f"></i>
                <% } else if (platform === 'linkedin') { %>
                  <i class="fab fa-linkedin-in"></i>
                <% } else if (platform === 'instagram') { %>
                  <i class="fab fa-instagram"></i>
                <% } else if (platform === 'weibo') { %>
                  <i class="fab fa-weibo"></i>
                <% } else if (platform === 'wechat') { %>
                  <i class="fab fa-weixin"></i>
                <% } else if (platform === 'qq') { %>
                  <i class="fab fa-qq"></i>
                <% } else if (platform === 'email') { %>
                  <i class="fas fa-envelope"></i>
                <% } else { %>
                  <i class="fas fa-link"></i>
                <% } %>
              </a>
            <% } %>
          </div>
        <% } %>
      </div>
      
      <div class="footer-section links">
        <h3 class="footer-title">快速链接</h3>
        <ul class="footer-links">
          <% for (let item in theme.menu) { %>
            <li>
              <a href="<%= url_for(theme.menu[item]) %>">
                <%= __(item) || item.charAt(0).toUpperCase() + item.slice(1) %>
              </a>
            </li>
          <% } %>
        </ul>
      </div>
      
      <div class="footer-section subscribe">
        <h3 class="footer-title">订阅更新</h3>
        <p>订阅最新文章通知</p>
        <div class="subscribe-form">
          <input type="email" placeholder="您的邮箱地址" class="subscribe-input">
          <button class="subscribe-button">订阅</button>
        </div>
        <% if (theme.footer && theme.footer.subscribe_description) { %>
          <p class="subscribe-description"><%= theme.footer.subscribe_description %></p>
        <% } else { %>
          <p class="subscribe-description">我们将定期发送最新文章和更新通知</p>
        <% } %>
      </div>
    </div> -->
    
    <div class="footer-bottom">
      <!-- 版权信息部分 -->
      <div class="copyright-section">
        <div class="copyright-text">
          &copy; <%= new Date().getFullYear() %> <%= config.title %>
        </div>
        <% if (theme.footer && theme.footer.copyright) { %>
          <div class="footer-copyright">
            <%= theme.footer.copyright %>
          </div>
        <% } else { %>
          <div class="footer-powered-by">
            由 <a href="https://hexo.io" target="_blank" rel="noopener noreferrer">Hexo</a> 驱动
          </div>
        <% } %>
      </div>
                
      <% if (theme.statistics && theme.statistics.enable) { %>
        <div class="site-statistics">
          <!-- 统计信息部分 -->
          <div class="stats-section">
            <!-- 运行时间 -->
            <% if (theme.statistics.runtime && theme.statistics.runtime.enable) { %>
              <div class="stat-item">
                <i class="fas fa-clock"></i>
                <span>运行时间</span>
                <span class="stat-value" id="site-runtime">0天0时0分0秒</span>
              </div>
            <% } %>
            
            <!-- 访问统计 -->
            <% if (theme.statistics.visitor && theme.statistics.visitor.enable) { %>
              <div class="visitor-stats">
                <% if (theme.statistics.visitor.show_pv) { %>
                  <div class="stat-item">
                    <i class="fas fa-eye"></i>
                    <span>访问量</span>
                    <span class="stat-value" id="busuanzi_value_site_pv">0</span>
                  </div>
                <% } %>
                
                <% if (theme.statistics.visitor.show_uv) { %>
                  <div class="stat-item">
                    <i class="fas fa-user"></i>
                    <span>访客数</span>
                    <span class="stat-value" id="busuanzi_value_site_uv">0</span>
                  </div>
                <% } %>
              </div>
            <% } %>
          </div>
        </div>
      <% } %>
      
      <div class="powered-by">
        <a href="https://github.com/yourusername/hexo-theme-wechat-style" target="_blank" rel="noopener noreferrer">
          微信风格主题
        </a>
      </div>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background-color: var(--footer-bg);
    padding: 50px 0 20px;
    margin-top: 50px;
    color: var(--text-color);
  }
  
  .footer-content {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 30px;
    margin-bottom: 30px;
  }
  
  .footer-title {
    font-size: 1.2rem;
    margin-bottom: 15px;
    color: var(--text-color);
    position: relative;
    padding-bottom: 10px;
  }
  
  .footer-title:after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 40px;
    height: 2px;
    background-color: var(--theme-color);
  }
  
  .footer-description {
    margin-bottom: 15px;
    color: var(--text-color-light);
    line-height: 1.6;
  }
  
  .social-links {
    display: flex;
    gap: 10px;
    margin-top: 20px;
  }
  
  .social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background-color: var(--theme-color-light);
    color: var(--theme-color);
    border-radius: 50%;
    transition: all 0.3s;
  }
  
  .social-link:hover {
    background-color: var(--theme-color);
    color: white;
    transform: translateY(-3px);
  }
  
  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .footer-links li {
    margin-bottom: 10px;
  }
  
  .footer-links a {
    color: var(--text-color-light);
    text-decoration: none;
    transition: color 0.2s;
  }
  
  .footer-links a:hover {
    color: var(--theme-color);
  }
  
  .subscribe-form {
    display: flex;
    margin-bottom: 10px;
  }
  
  .subscribe-input {
    flex: 1;
    padding: 10px;
    border: 1px solid var(--border-color);
    border-radius: 4px 0 0 4px;
    font-size: 0.9rem;
    background-color: var(--bg-color);
    color: var(--text-color);
  }
  
  .subscribe-button {
    padding: 10px 15px;
    background-color: var(--theme-color);
    color: white;
    border: none;
    border-radius: 0 4px 4px 0;
    cursor: pointer;
    font-size: 0.9rem;
    transition: background-color 0.2s;
  }
  
  .subscribe-button:hover {
    background-color: #06a050;
  }
  
  .subscribe-description {
    font-size: 0.8rem;
    color: var(--text-color-lighter);
    margin-top: 10px;
  }
  
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 20px;
    /* border-top: 1px solid var(--border-color); */
    font-size: 0.9rem;
    color: var(--text-color-lighter);
    flex-wrap: wrap;
    gap: 10px;
  }
  
  .footer-bottom a {
    color: var(--theme-color);
    text-decoration: none;
  }
  
  .runtime {
    margin-right: 20px;
  }
  
  .visitor-stats {
    display: flex;
    gap: 70px;
  }
  
  .site-pv, .site-uv {
    display: flex;
    align-items: center;
  }
  
  .site-pv i, .site-uv i {
    margin-right: 5px;
  }
  
  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
    }
    
    .footer-bottom {
      flex-direction: column;
      text-align: center;
    }
  }
</style>

<!-- 运行时间计算脚本 -->
<% if (theme.statistics && theme.statistics.enable && theme.statistics.runtime && theme.statistics.runtime.enable) { %>
<script>
document.addEventListener('DOMContentLoaded', function() {
  function updateRuntime() {
    const startDate = new Date('<%= theme.statistics.runtime.date %>');
    const now = new Date();
    const timeDiff = now - startDate;
    
    const days = Math.floor(timeDiff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((timeDiff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((timeDiff % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((timeDiff % (1000 * 60)) / 1000);
    
    document.getElementById('site-runtime').textContent = 
      `${days} 天 ${hours} 小时 ${minutes} 分 ${seconds} 秒`;
  }
  
  updateRuntime();
  setInterval(updateRuntime, 1000);
});
</script>
<% } %>

<!-- 访问统计脚本 -->
<% if (theme.statistics && theme.statistics.enable && theme.statistics.visitor && theme.statistics.visitor.enable) { %>
<script async src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"></script>
<% } %>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    
    // Handle subscribe button click
    var subscribeButton = document.querySelector('.subscribe-button');
    if (subscribeButton) {
      subscribeButton.addEventListener('click', function() {
        var emailInput = document.querySelector('.subscribe-input');
        var email = emailInput.value.trim();
        
        if (email && /^\S+@\S+\.\S+$/.test(email)) {
          alert('感谢您的订阅！');
          emailInput.value = '';
        } else {
          alert('请输入有效的邮箱地址');
        }
      });
    }
  });
</script>

<!-- 在页面底部引入脚本，确保 DOM 已完全加载 -->
<%- js('js/pagination.js') %>